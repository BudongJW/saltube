__pycache__/
*.pyc
.venv/
.DS_Store

# 영상 원본·편집 프로젝트는 리포에 넣지 않습니다 (용량).
# 별도 클라우드 스토리지에 이중 백업 — docs/09-risk-and-compliance.md §3
*.mp4
*.mov
*.wav
*.aep
*.prproj

# 제작 패키지는 대본에서 생성되는 파생물 — 커밋하지 않습니다.
# CI가 빌드해 워크플로 아티팩트로 올립니다 (.github/workflows/produce.yml)
build/

# 렌더 산출물 (build/ 에 생성)
*-draft.mp4

# TTS 산출물 (build/ 에 생성)
*.mp3

# fetchmedia 검색 캐시
.fetchmedia-last.json
